[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: every path serves index.html so /intake, /carriers etc. load.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
